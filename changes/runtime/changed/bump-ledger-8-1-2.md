# Bump ledger 8 to 8.1.2

Bumps `midnight-ledger`/`midnight-zswap` to 8.1.2, pulling the rest of the shared
2.x stack forward: `midnight-storage` 2.0.3, `midnight-storage-core` 1.2.1,
`midnight-onchain-runtime` 3.1.1 and `midnight-serialize` 1.1.1.
