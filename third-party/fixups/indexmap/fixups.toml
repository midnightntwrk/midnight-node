# Local override: indexmap-2 has no build script (the shared has_std/buildscript
# config targets indexmap-1.x). Just drop `std` on wasm32 for the no_std runtime.
['cfg(target_arch = "wasm32")']
omit_features = ["std"]
