[package]
name = "pallet-midnight-rpc"
version = "0.1.0"
edition = "2024"
license-file.workspace = true

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
jsonrpsee  = { workspace = true, features = [ "client", "server", "macros" ] }
serde     .workspace = true
hex.workspace = true

sc-client-api.workspace = true
sp-api.workspace = true
sp-runtime.workspace = true
sp-blockchain.workspace = true
sp-storage.workspace = true
parity-scale-codec = { workspace = true, features = ["derive", "std"] }
pallet-midnight = { workspace = true, features = ["std"] }
midnight-node-ledger = { workspace = true, features = ["std"] }
midnight-primitives = { workspace = true, features = ["std"] }
base-crypto.workspace = true
ledger-storage.workspace = true

[dev-dependencies]

[lints]
workspace = true
